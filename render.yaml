services:
  - type: web
    name: univet-order-export
    env: python
    buildCommand: bash render_build.sh
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
